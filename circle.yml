machine:
  environment:
    CONTAINER_NAME: quay.io/redsift/javaos
    CIRCLE_REVERSE_DEPENDENCIES: webhook
  services:
    - docker

checkout:
  post:
    - git fetch --tags

dependencies:
  override:
    - docker info
    - |
        sudo docker build -t $CONTAINER_NAME .
        VERSION=$(git describe --exact-match --tags 2>/dev/null || git rev-parse --short HEAD)
        echo "Tagging as $CONTAINER_NAME:$VERSION"
        docker tag $CONTAINER_NAME:latest $CONTAINER_NAME:$VERSION

test:
  override:
    - "docker run -a stdout -a stderr --entrypoint=java $CONTAINER_NAME -version"

deployment:
  prod:
    branch: master
    commands:
      - docker login -e $QUAY_EMAIL -u $QUAY_USER -p $QUAY_PASS quay.io
      - docker push $CONTAINER_NAME
      - "./circle_ci_trigger_build"
